steps:
- task: PublishPipelineArtifact@1
  inputs:
   artifact: '${{parameters.outName}}'
  #  artifactName: 'sqlArtifact'
   targetPath: '${{parameters.outPath}}\${{parameters.outName}}'
   publishLocation: 'pipeline'
  displayName: 'Publish: ${{parameters.outName}}'